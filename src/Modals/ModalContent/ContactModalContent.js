import ContactForm from '../../Forms/ContactForm';

const ContactModalContent = () => {
  return (
    <section>
      <h1>Contact</h1>
      <p>
        I'm always open to chatting. Whether you have a question to ask or an
        exciting opportunity to share, don't hesitate to ping me a message.
      </p>
      <ContactForm />
    </section>
  );
};

export default ContactModalContent;
